version: 2.1

jobs:
    build:
      docker:
        - image: prashantsail/pytorch-serve-build
      steps:
        - checkout
        - run:
            name: Build pytorch serve
            command: python setup.py bdist_wheel --release --universal

workflows:
  version: 2
  smoke:
    jobs:
      - build
